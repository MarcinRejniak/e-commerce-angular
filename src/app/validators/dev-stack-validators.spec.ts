import { DevStackShopValidators } from './dev-stack-shop-validators';

describe('DevStackValidators', () => {
  it('should create an instance', () => {
    expect(new DevStackShopValidators()).toBeTruthy();
  });
});
